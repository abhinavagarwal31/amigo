GEMINI_API_KEY=your_gemini_api_key_here
PORT=3001
FRONTEND_ORIGIN=http://localhost:5173
# Which venue this kiosk build is deployed at (must match an id in backend/data/venues.json).
# Falls back to the first known venue if unset or invalid.
VITE_KIOSK_VENUE_ID=venue_01
